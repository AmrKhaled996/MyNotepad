name: test_work_sapce
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  flutter_local_notifications: ^19.1.0
  share_plus: ^11.0.0
  intl: ^0.18.1
  timezone: ^0.10.1
  rxdart: ^0.28.0

dev_dependencies:
  build_runner: ^2.4.0
  hive_generator: ^2.0.1

flutter:
  uses-material-design: true
